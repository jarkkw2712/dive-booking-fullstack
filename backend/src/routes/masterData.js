import express from "express";import { requireAuth } from "../middleware/auth.js";import { supabaseAdmin } from "../services/supabase.js";
const router=express.Router();router.use(requireAuth);
router.get("/",async(req,res)=>{const tables={programs:"master_programs",addOns:"master_addons",accommodations:"master_accommodations",customerSources:"master_customer_sources",transportationMethods:"master_transportation_methods",paymentMethods:"master_payment_methods"};const entries=await Promise.all(Object.entries(tables).map(async([key,table])=>[key,await supabaseAdmin.from(table).select("*").eq("active_flag",true).order("sort_order")]));const failed=entries.find(([,result])=>result.error);if(failed)return res.status(500).json({error:failed[1].error.message});res.json(Object.fromEntries(entries.map(([key,result])=>[key,result.data||[]])))});
export default router;
