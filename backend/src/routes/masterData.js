import express from "express";import { requireAuth } from "../middleware/auth.js";import { supabaseAdmin } from "../services/supabase.js";
const router=express.Router();router.use(requireAuth);
router.get("/",async(req,res)=>{const tables={programs:"master_programs",addOns:"master_addons",accommodations:"master_accommodations",customerSources:"master_customer_sources",transportationMethods:"master_transportation_methods",paymentMethods:"master_payment_methods"},fallbacks={customerSources:[{source_id:"existing_customer",source_name:"ลูกค้าเก่า"},{source_id:"facebook",source_name:"Facebook"},{source_id:"agent",source_name:"Agent"},{source_id:"walk_in",source_name:"Walk-in"}],transportationMethods:[{method_id:"private_car",method_name:"รถยนต์ส่วนตัว"},{method_id:"van",method_name:"รถตู้"},{method_id:"coach",method_name:"รถทัวร์"}]};const entries=await Promise.all(Object.entries(tables).map(async([key,table])=>[key,await supabaseAdmin.from(table).select("*").eq("active_flag",true).order("sort_order")]));const failed=entries.find(([key,result])=>result.error&&!fallbacks[key]);if(failed)return res.status(500).json({error:failed[1].error.message});res.json(Object.fromEntries(entries.map(([key,result])=>[key,result.error?fallbacks[key]:result.data||[]])))});
export default router;
