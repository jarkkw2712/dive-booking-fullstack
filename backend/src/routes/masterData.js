import express from "express";import { requireAuth } from "../middleware/auth.js";import { supabaseAdmin } from "../services/supabase.js";
const router=express.Router();router.use(requireAuth);
router.get("/",async(req,res)=>{const [p,a,h]=await Promise.all([supabaseAdmin.from("master_programs").select("*").eq("active_flag",true).order("sort_order"),supabaseAdmin.from("master_addons").select("*").eq("active_flag",true).order("sort_order"),supabaseAdmin.from("master_accommodations").select("*").eq("active_flag",true).order("sort_order")]);if(p.error)return res.status(500).json({error:p.error.message});if(a.error)return res.status(500).json({error:a.error.message});if(h.error)return res.status(500).json({error:h.error.message});res.json({programs:p.data||[],addOns:a.data||[],accommodations:h.data||[]})});
export default router;
