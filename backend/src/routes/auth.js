import express from "express";import jwt from "jsonwebtoken";import { supabaseAdmin } from "../services/supabase.js";import { requireAuth } from "../middleware/auth.js";
const router=express.Router();
async function perms(role){const {data,error}=await supabaseAdmin.from("role_permissions").select("permission_key,allowed").eq("role_id",role);if(error)throw error;const p={};(data||[]).forEach(x=>p[x.permission_key]=x.allowed);return p}
router.post("/login",async(req,res)=>{const {username,password}=req.body;const {data:u,error}=await supabaseAdmin.from("app_users").select("user_id,username,display_name,role_id,active_flag").eq("username",username).maybeSingle();if(error)return res.status(500).json({error:error.message});if(!u||!u.active_flag)return res.status(401).json({error:"User inactive/not found"});if(password!==(process.env.DEMO_PASSWORD||"1234"))return res.status(401).json({error:"Invalid password"});const permissions=await perms(u.role_id);const payload={userId:u.user_id,username:u.username,displayName:u.display_name,role:u.role_id,permissions};const token=jwt.sign(payload,process.env.JWT_SECRET||"dev-secret",{expiresIn:"12h"});res.json({token,user:payload})});
router.get("/me",requireAuth,(req,res)=>res.json({user:req.user}));
export default router;
